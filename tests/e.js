views('alala', 'etr' + 'wtf' + 5);

views('bebebe', views('etf', {e:1}) + 'next');

views('cdcdcd', 'begin' +  views('qqq', {q:0}));

function badIdea(hash) {
    var bla;
    
    var t = views('badd', {
            a: 1
        });
    
    return views('whop', {
        wtf: 42
    }) + views('wla', bla);
};

views('dfgsdf', wtf.text);

var name = 'wqeqwqe';
views('sdfas', function() {
    var x = views(name, {dfg:4});

    return 'ok';
});
